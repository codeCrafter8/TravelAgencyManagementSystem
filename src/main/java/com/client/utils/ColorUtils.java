package com.client.utils;

import java.awt.*;

/**
 * The ColorUtils class provides a set of predefined colors that can be used in the application's user interface.
 * This class contains static final Color objects representing various color options for the UI elements.
 * All colors are represented using the RGB color model.
 */
public final class ColorUtils {

    /**
     * Private constructor to prevent instantiation of the utility class.
     */
    private ColorUtils() {}

    /**
     * A light beige color represented in the RGB color model.
     */
    public static final Color LIGHT_BEIGE = new Color(247, 233, 201);

    /**
     * A milk white color represented in the RGB color model.
     */
    public static final Color MILK = new Color(242, 242, 242);

    /**
     * A dark beige color represented in the RGB color model.
     */
    public static final Color DARK_BEIGE = new Color(189, 165, 111);

    /**
     * A cream color represented in the RGB color model.
     */
    public static final Color CREAM = new Color(241, 227, 185);

    /**
     * A light grey color represented in the RGB color model.
     */
    public static final Color LIGHT_GREY = new Color(190, 190, 192);

    /**
     * A grey color represented in the RGB color model.
     */
    public static final Color GREY = new Color(153, 153, 153);

    /**
     * A beige color represented in the RGB color model.
     */
    public static final Color BEIGE = new Color(215, 198, 151);

    /**
     * A light brown color represented in the RGB color model.
     */
    public static final Color LIGHT_BROWN = new Color(151, 123, 92);

    /**
     * A brown color represented in the RGB color model.
     */
    public static final Color BROWN = new Color(118, 98, 75);

    /**
     * A nude color represented in the RGB color model.
     */
    public static final Color NUDE = new Color(175, 157, 121);
}
